server:
  port: 8080
spring:
  datasource:
    url: jdbc:mariadb://${DB_IP:localhost}:${DB_PORT:3344}/dgsw_26_1st_01
    username: ${DB_USER}
    password: ${DB_PWD}
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true